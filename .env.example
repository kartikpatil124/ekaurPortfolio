# Server Configuration
PORT=5000

# MongoDB Atlas Connection String (replace with your MongoDB Atlas URI)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/portfolio_db?retryWrites=true&w=majority

# Session Secret (use a strong random string in production)
SESSION_SECRET=your_super_secret_key_here_change_this_in_production

# Admin Credentials
ADMIN_EMAIL=admin@portfolio.com
ADMIN_PASSWORD=admin123

# Node Environment
NODE_ENV=production
